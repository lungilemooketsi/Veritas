{
  "name": "@veritas/subgraph",
  "version": "1.0.0",
  "description": "The Graph Subgraph for Veritas Marketplace",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio veritas-marketplace",
    "deploy:hosted": "graph deploy --product hosted-service veritas/veritas-marketplace",
    "create-local": "graph create --node http://localhost:8020/ veritas-marketplace",
    "remove-local": "graph remove --node http://localhost:8020/ veritas-marketplace",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 veritas-marketplace"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.64.0",
    "@graphprotocol/graph-ts": "^0.32.0"
  }
}
